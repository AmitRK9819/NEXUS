# Core API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9
httpx>=0.27

# NLP / structuring
vaderSentiment>=3.3.2
spacy>=3.7
# after installing spacy, also run:
#   python -m spacy download en_core_web_sm
langdetect>=1.0.9

# Speech-to-text (choose one; faster-whisper recommended)
faster-whisper>=1.0.0
# Alternative: openai-whisper
# Alternative for max accuracy on supported languages: NVIDIA NeMo + Canary-Qwen-2.5B

# Testing
pytest>=8.0
