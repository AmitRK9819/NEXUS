# NEXUS Platform — Environment Configuration
# Copy this file to .env and update values for your local setup.

# PostgreSQL connection string (requires PostGIS extension)
DATABASE_URL=postgresql://nexus_user:nexus_pass@localhost:5432/nexus_db
